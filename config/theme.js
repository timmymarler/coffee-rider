export const theme = {
  colors: {
    // Main background + surfaces
    background: "#0E1217",      // deep charcoal with blue undertone
    surface: "#162028",         // muted slate-blue for cards/inputs

    // Primary blues
    primary: "#2E4A68",         // muted pastel dark blue (your main brand colour)
    primaryLight: "#4F6E8D",    // softer blue for highlights
    primaryExtraLight: "#7190AC", // pastel, gentle blue for subtle UI

    // Accent / “Coffee Rider Gold”
    accent: "#C8A46D",          // pale brown / gold hint
    accentLight: "#E4CFA3",     // soft latte tone for highlights

    // Text
    text: "#E8EDF2",            // soft off-white
    textMuted: "#8D99A6",       // gentle muted grey-blue
    placeholder: "#C8B89A",     // pale warm coffee tone

    // Status
    success: "#70C17C",
    warning: "#D9A441",
    danger: "#D2675A",

    // Borders / separators
    border: "#22303C",
  },

  spacing: {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 32
  },

  radius: {
    sm: 6,
    md: 10,
    lg: 16,
    round: 999
  }
};
